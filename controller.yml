resources:
  - name: source_yaml
    type: GitRepo
    configuration:
      gitProvider: gitlab
      path: automation/custom_certs


pipelines:
  - name: controller
    configuration:
      nodePool: trigger_pool
    steps:
      - name: first
        type: TriggerPipeline
        configuration:
          integrations:
            - name: platform_token
          pipelineName: pipeline_S_DockerPush_5933_004
          stepName: S_DockerPush_5933_004_1
